{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cdn.jsdelivr.net/gh/jasocami/realistic-test-data@v0.1.0/schemas/finance/cards.schema.json",
  "title": "finance/cards",
  "description": "One row per payment card issued against one account. Debit and credit cards. Numbers come from the published test ranges that payment processors treat as non-live, so they cannot move money while still passing the Luhn check that a checkout form applies.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "Surrogate primary key, sequential from 1.",
        "examples": [
          1
        ]
      },
      "account_id": {
        "type": "integer",
        "description": "The account this card draws on. One account may have several cards, for instance a debit card and a credit card.",
        "examples": [
          42
        ]
      },
      "card_number_masked": {
        "type": "string",
        "description": "The card number as an interface should display it, with the middle digits hidden. Never store or show a full number -- PCI DSS forbids it, and this column is what a compliant UI renders.",
        "examples": [
          "4111 11** **** 1111"
        ]
      },
      "card_number_last4": {
        "type": "string",
        "pattern": "^\\d{4}$",
        "description": "Final four digits, the standard way to let somebody identify which of their cards this is without exposing the rest.",
        "examples": [
          "1111"
        ]
      },
      "brand": {
        "type": "string",
        "enum": [
          "American Express",
          "Mastercard",
          "Visa"
        ],
        "description": "Card network. The brand determines the leading digits: Visa always starts with 4, Mastercard with 5 or 2, American Express with 37.",
        "examples": [
          "Visa"
        ]
      },
      "card_type": {
        "type": "string",
        "enum": [
          "debit",
          "credit",
          "prepaid"
        ],
        "description": "Whether spending draws directly on the account, on a credit line, or on a preloaded balance.",
        "examples": [
          "debit"
        ]
      },
      "expires_on": {
        "type": "string",
        "format": "date",
        "description": "Expiry date, always the last day of a month because that is how card expiry works. Cards whose date has passed carry the status 'expired'.",
        "examples": [
          "2027-08-31"
        ]
      },
      "credit_limit": {
        "type": [
          "number",
          "null"
        ],
        "description": "Spending limit for credit cards, scaled to the customer's declared income. Empty for debit cards, which have no limit of their own.",
        "examples": [
          3000.0
        ]
      },
      "status": {
        "type": "string",
        "enum": [
          "active",
          "blocked",
          "cancelled",
          "expired"
        ],
        "description": "Current state of the card. Blocked cards were reported lost or frozen for fraud; expired ones simply ran out of time.",
        "examples": [
          "active"
        ]
      },
      "issued_at": {
        "type": "string",
        "format": "date",
        "description": "Date the card was issued, always on or after the account was opened and roughly four years before it expires.",
        "examples": [
          "2023-08-14"
        ]
      }
    },
    "required": [
      "id",
      "account_id",
      "card_number_masked",
      "card_number_last4",
      "brand",
      "card_type",
      "expires_on",
      "status",
      "issued_at"
    ],
    "additionalProperties": false
  }
}
